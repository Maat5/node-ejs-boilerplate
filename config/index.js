var libs = require('./libs');
var styles = require('./styles');
var fonts = require('./fonts');

var config = {
  src: 'app',
  libs: libs,
  styles: styles,
  fonts: fonts
};

module.exports = config;